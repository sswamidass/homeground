{
  "name": "homeground",
  "private": true,
  "workspaces": ["frontend"],
  "scripts": {
    "start": "cd frontend && npm run dev",
    "build": "cd frontend && npm run build",
    "preview": "cd frontend && npm run preview"
  }
}
